# 右手専用：セントラル側（PC接続）

# 分割キーボード：セントラル
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

# BLE接続
CONFIG_ZMK_BLE=y

# ポインティングデバイス（TrackPoint）
CONFIG_ZMK_POINTING=y
CONFIG_INPUT=y
CONFIG_PS2_INIT_PRIORITY=5

# ログオフ（本番運用時）
CONFIG_LOG=n

# BLE送信電力を下げる（CPU負荷軽減）
CONFIG_BT_CTLR_TX_PWR_MINUS_20=y
